name: if_wishes_could_kill
description: Flutter web deployment test
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  path_provider: ^2.1.3
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^16.3.0
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
